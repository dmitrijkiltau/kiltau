import * as React from 'react'
import Layout from '../components/layout'

const MinigamesPage = ({location}) => {
  return (
    <Layout pageTitle="Minigames" slug={location.pathname}>
    </Layout>
  )
}

export default MinigamesPage
