import * as React from 'react'
import Layout from '../components/layout'

const ToolsPage = ({location}) => {
  return (
    <Layout pageTitle="Tools" slug={location.pathname}>
    </Layout>
  )
}

export default ToolsPage
