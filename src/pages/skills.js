import * as React from 'react'
import Layout from '../components/layout'

const SkillsPage = ({location}) => {
  return (
    <Layout pageTitle="Skills" slug={location.pathname}>
    </Layout>
  )
}

export default SkillsPage
